# Intentionally empty (personal-cfo-r36ck, ADR 0082 decision 6).
#
# Until this move, this file declared `export-ignore` for seventeen paths —
# maintainer-only bead-database backup tooling and session-handoff notes —
# so `git archive`, the mechanism behind the periodic public snapshots
# (docs/operations/public-launch-snapshot.md), never carried them into the
# public repository. Those files now live in the private dohflow/internal
# repository instead, and development happens directly in this repository
# from here on — there are no more snapshots for export-ignore to protect.
