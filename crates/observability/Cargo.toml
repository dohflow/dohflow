[package]
name = "observability"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "Logging redaction layer for DohFlow: a redact() backstop and a tracing-subscriber layer that scrubs every log line before it reaches a sink, so logs are safe to share (plan §6.6)."

[dependencies]
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
regex = { workspace = true }
